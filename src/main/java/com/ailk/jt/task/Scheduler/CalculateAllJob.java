package com.ailk.jt.task.Scheduler;

/*import org.apache.log4j.Logger;
import org.quartz.Job;
import org.quartz.JobExecutionContext;
import org.quartz.JobExecutionException;

import com.ailk.jt.task.biz.CalculateAll;*/

@Deprecated
public class CalculateAllJob /*implements Job*/ {
	/*private static final Logger log = Logger.getLogger(CalculateAllJob.class);// 获得日志打印对象

	public void execute(JobExecutionContext arg0) throws JobExecutionException {
		log.debug(" begin calculate All ......");
		CalculateAll.main(null);
		log.debug(" end calculate All ...");
	}*/
}
